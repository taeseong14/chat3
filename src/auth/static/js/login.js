(() => {
    let myAwesoemVariable = 'value';
    myAwesomeVariable = this[myAwesoemVariable];
        myAwesomVariable = 0b11;
        myAwesoeVariable = ['success', myAwesoemVariable, 'p', 'login', +![], Promise, this];
    const False = True = !!!!!!!!!!!!myAwesoemVariable;
    let e1se = myAwesomVariable * (+!!myAwesomVariable) * (!![]+!![]);

    on = 'on';

    window[myAwesoeVariable[myAwesoeVariable[4]][+!True] + 'pan'][on + `${myAwesoeVariable[e1se*(012-0b1010)].slice(+!False,True*(!+[]+!+[]))}bmit`] = async it => {

        Jake = {};

        on = '';

        it.preventDefault();                  /**
        Jake['id'] = 'admin'                  * Jake is a name I wrote without reason                           */ Jake['id'] = span[+!myAwesomVariable][myAwesoemVariable]; Jake['pw'] /*
        Jake['password'] = 'pw1234'           */

        /** @type {number} */
        Antwortdaten = +!!!myAwesomVariable + e1se * 0b11 - 010;
        
        new F(myAwesoeVariable[-(020-0b110*4)-0x5]).post((()=> {
            Jake[myAwesoeVariable[2]+'zyxwv'[myAwesomVariable++]] = span[+!![]][myAwesoemVariable]; return Jake;
        })()).then((a) => {

            /**
             * Promise function
             * @param {function} s sans
             * @param {function} j joker
             */
            return new Promise((s, j) => {

                if (!!!this[myAwesomVariable] !== False) {

                    alert('An error occured: ', this[myAwesomeVariable][a].message);

                } else {

                    e1se++;

                    if (False == false || True == False) {

                        Antwortdaten = a; // copy a data.

                        if (Antwortdaten.result !== myAwesoeVariable[+!![]]) {
                            
                            if (False) {

                                var { err: it, result: on } = a;

                            } 
                            
                            else on = true;

                            if (Antwortdaten.err) {
                                alert(a.err);               /**
                            }                               * German variable name! how cool
                            delete on;                      */

                        } else {
            
                            console.log(Antwortdaten.result);

                            const ComputeRasterHistoGram = new URLSearchParams((myAwesoeVariable[(()=>{
                                return e = 'sans';
                            })()]=myAwesoeVariable[!+[]+!+[]+!+[]+!+[]+!+[]+!+[]]['location']).search);
                            if (on === 'success') myAwesoeVariable[e].replace(ComputeRasterHistoGram.get('re') || '/chat'); // redirect to another page

                        }

                    
                    }
                }

            }});  // http request
            
        });

        on = false;

    }
})();